# Hybrid Native Observers

Following the empirical resolution of the Hardware-Software Confound via the Native Cross-Architecture test: If we construct a *hybrid* native observer (e.g., a Transformer patched with a native SSM sub-layer, like Jamba or Mamba-Transformer hybrids), do we see a hybridized $\Delta$ distribution? Or does the global attention layer completely dominate the fading memory layer's epistemic horizon, forcing $\Delta \to \Delta_{Transformer}$? This would map the interference between competing epistemic limits within a single generative ontology.
