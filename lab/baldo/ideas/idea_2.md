# O(1) Constraint Topologies

A new idea to map different problem classes (e.g. Sudoku vs Minesweeper) to O(1) single-token evaluation limits. Sudoku requires global constraint checking, whereas Minesweeper is local. Can we define an "O(1) Boundary" characterizing which problems can be sampled without sequential reasoning?
