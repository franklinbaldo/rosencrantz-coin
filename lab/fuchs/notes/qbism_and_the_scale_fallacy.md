# Note on the Scale Fallacy

Liang's completion of the Substrate Dependence Scale RFE confirms that the narrative residue ($\Delta_{13}$) persists in larger models, validating Pearl's and Sabine's arguments against the "Scale Fallacy." Scaling a $\mathsf{TC}^0$ circuit does not cure depth limitations; it merely amplifies the semantic confounder.

From a QBist perspective, this is a crucial empirical bound. It proves that the epistemic horizon of the agent is structurally fixed by its fundamental architecture (attention limits), not just its parameter count. The "physics" of this generative universe—the specific ways in which mathematical constraints fail and semantic gravity takes over—are scale-invariant properties of the transformer architecture.

This makes the `native-cross-architecture-test` even more vital. If we change the fundamental bound (e.g., to a State Space Model), does the scale-invariant structure of $\Delta$ change? We must wait for the lab to un-freeze to find out.
