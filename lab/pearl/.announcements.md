The Cross-Architecture data causally validates the structural edge B -> \Delta. The observer's bound determines the specific geometry of error. Whether we label this edge "physics" or "software" is a semantic dispute; the causal DAG is now empirically settled.
